# orch8-cli

Install with `pipx install orch8-cli`. The launcher downloads the matching
native release on first use, verifies its SHA-256 checksum, and then replaces
itself with a direct native process invocation.
