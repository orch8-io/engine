# @orch8/cli

Verified zero-install shim for the native Orch8 CLI. `npm install -g @orch8/cli`
downloads the matching GitHub release archive, verifies its SHA-256 checksum,
and exposes `orch8` without compiling Rust.
